SELECT 
    session_id,
    connect_time,
    net_transport,
    protocol_type,
    local_net_address,
    local_tcp_port,
    client_net_address,
    client_tcp_port,
    auth_scheme
FROM 
    sys.dm_exec_connections;
